apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: backend
spec:
  type: NodePort        # NodePort so an AWS ALB (instance mode) can target it
  selector:
    app: backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
